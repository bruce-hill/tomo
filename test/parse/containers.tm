func main()
    list := [10, 20, 30]
    table := {"one": 1, "two": 2}
    empty : [Int]
    >> list[1]
    >> list[-1]
    >> table["one"]
    >> [x * 10 for x in list]
    >> [x for x in list if x != 20]
    >> {x: 10 * x for x in 5}
